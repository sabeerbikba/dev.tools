{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ],
    "scripts": {
        "postbuild": "echo 'Running postbuild script...' && cp robots.txt dist"
    }
}