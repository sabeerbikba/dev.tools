{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ],
    "scripts": {
        "postbuild": "echo 'Running postbuild script...' && shx cp $VERCEL_PROJECT_ROOT/robots.txt $VERCEL_PROJECT_ROOT/dist"
    }
}