{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/index.html"
        }
    ],
    "scripts": {
        "postbuild": "echo 'Running postbuild script...' && shx cp robots.txt dist"
    }
}